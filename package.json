{
  "name": "agentblazer-buildblazer",
  "version": "1.0.0",
  "private": true,
  "description": "AgentBlazer Club website (frontend + backend) - run everything from this folder",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "create-admin": "cd backend && node scripts/createAdmin.js",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
